name: Filter Types
on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - main
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
     - run: echo "This workflow will only run for pull requests"
